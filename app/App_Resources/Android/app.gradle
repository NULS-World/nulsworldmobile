// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "world.nuls.mobile"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
